export function groupParticipationRecords(submissions,sessions=[]){const groups=new Map();for(const session of sessions)groups.set(session.id,{sessionId:session.id,gradeName:session.gradeName||"",className:session.className||"",teamName:session.teamName||"",studentNumber:session.studentNumber||"",studentName:session.studentName||"",visibleToPeers:session.visibleToPeers!==false,createdAt:session.createdAt,submissions:[]});for(const submission of submissions){const sessionId=submission.sessionId||`legacy-${submission.id}`;if(!groups.has(sessionId))groups.set(sessionId,{sessionId,gradeName:submission.gradeName||"",className:submission.className||"",teamName:submission.teamName||"",studentNumber:submission.studentNumber||"",studentName:submission.studentName||"",visibleToPeers:submission.visibleToPeers!==false,submissions:[]});groups.get(sessionId).submissions.push(submission);}return [...groups.values()].map(record=>({...record,submissions:record.submissions.sort((a,b)=>Number(a.questionIndex)-Number(b.questionIndex))})).sort((a,b)=>timestamp(b.createdAt||b.submissions[0]?.submittedAt)-timestamp(a.createdAt||a.submissions[0]?.submittedAt));}
export function createAdminPopupPage(submission,feedback){return {...submission,submissionType:submission.submissionType||"photo",editAllowance:Number(submission.editAllowance||0),feedback:feedback.filter(item=>item.submissionId===submission.id)};}
function timestamp(value){return value?.toMillis?.()||Number(value?.seconds||0)*1000;}
