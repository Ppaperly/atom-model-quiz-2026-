import test from "node:test";import assert from "node:assert/strict";import {readFile} from "node:fs/promises";
test("administrator card list has filters layouts and responsive columns",async()=>{const script=await readFile("src/admin-card-list.js","utf8");const styles=await readFile("feature-styles.css","utf8");assert.match(script,/전체 조회/);assert.match(script,/전체 배치/);assert.match(script,/조별 배치/);assert.match(script,/번호순 배치/);assert.match(script,/participant-card-menu-button/);assert.match(styles,/grid-template-columns:repeat\(4/);assert.match(styles,/grid-template-columns:repeat\(2/);assert.match(styles,/aspect-ratio:1/);});
